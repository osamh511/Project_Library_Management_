# Project_Library_Management_
يعتبر هذا المشروع نظاماً برمجياً متكاملاً لإدارة العمليات اليومية للمكتبات، تم بناؤه باستخدام لغة VB.NET كواجهة أمامية وقاعدة بيانات SQL Server كخلفية لإدارة البيانات 


📚 Advanced Library Management System (ALMS)نظام إدارة المكتبة المتطور - بنية هندسية متكاملة باستخدام VB.NET و SQL Server.🌟 نظرة عامة (Overview)هذا المشروع ليس مجرد تطبيق لإدارة الكتب، بل هو نموذج تطبيقي متطور يجسد مفاهيم هندسة البرمجيات (Software Engineering). تم بناؤه باستخدام لغة VB.NET مع الاعتماد على SQL Server كقاعدة بيانات، مع تطبيق صارم لمبادئ SOLID لفصل المسؤوليات وتسهيل التوسع المستقبلي.🚀 المميزات التقنية (Technical Features)⚡ نظام إعارة ذكي: معالجة عمليات الإعارة والإرجاع مع تحديث تلقائي وفوري للمخزون البرمجي.🛡️ أمان معتمد على SQL: نظام تسجيل دخول محصن يعتمد على Stored Procedures لمنع هجمات الحقن (SQL Injection).🔍 محرك بحث متقدم: فلترة ذكية للكتب والأعضاء باستخدام استعلامات SQL محسنة.💾 استراتيجيات بيانات مزدوجة:Connected Mode: لاسترجاع البيانات بسرعة كأجسام (Objects) باستخدام SqlDataReader.Disconnected Mode: لعرض الجداول الضخمة باستخدام DataSet و DataAdapter.📝 نظام تدوين الأخطاء (Logging): كلاس مركزية تقوم بحفظ الأخطاء برمجياً في ملفات نصية لتسهيل عملية الـ Debugging.🏗️ التصميم الهندسي (System Architecture)تم تصميم النظام ليكون قابلاً للاختبار (Testable) والصيانة (Maintainable) من خلال:1. Repository Pattern & Interfacesالواجهات (Interfaces) مثل IBooksRepository تعمل كعقد (Contract) يفصل بين واجهة المستخدم وطريقة تخزين البيانات.2. Dependency Injection (DI)استخدام Constructor Injection لتمرير المستودعات للفورمات، مما يلغي الاعتماد المباشر (Tight Coupling).3. Business Logic Layer (BLL)تحقق صارم من البيانات عبر كلاسات Validator قبل وصولها لقاعدة البيانات لضمان سلامة المدخلات.🛠️ التقنيات المستخدمة (Tech Stack)المجالالتقنية المستخدمةلغة البرمجةVB.NET (.NET Framework)قاعدة البياناتMicrosoft SQL Serverالوصول للبياناتADO.NET (Connected & Disconnected)المعماريةRepository Pattern, Layered ArchitectureالأمنStored Procedures, Transactions (ACID)📂 هيكلة قاعدة البيانات (Database Logic)يتميز المشروع باستخدام تقنيات متقدمة داخل SQL Server:Transactions: لضمان تنفيذ عمليات الإعارة بالكامل أو إلغائها في حال حدوث خطأ (All or Nothing).Stored Procedures: مثل sp_CheckLogin و sp_BorrowBook لزيادة الأداء وحماية البيانات.LINQ Projection: لرفع كفاءة الذاكرة عبر جلب الأعمدة المطلوبة فقط.⚙️ البدء بالعمل (Installation & Setup)قاعدة البيانات: قم بتنفيذ سكربت SQL المرفق لإنشاء LibraryDB.إعداد الاتصال: افتح ملف App.config وقم بتغيير connectionString:
